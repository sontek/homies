[user]
	name = Sean Talts
	email = sean@lot18.com
[color]
    diff = auto
    status = auto
    branch = auto

[push]
    default = matching

[core]
	editor = 'vim'

[merge]
    tool = mvim

[mergetool "mvim"]
    cmd=mvim -d -g $LOCAL $MERGED $REMOTE
    keepbackup=false
