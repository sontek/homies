[user]
	name = Sean Talts
	email = sean@lot18.com
[color]
    diff = auto
    status = auto
    branch = auto

[push]
    default = matching

[core]
	editor = ~/bin/vim

[merge]
    tool = mvim

[mergetool "mvim"]
    cmd=mvim -d -g $LOCAL $MERGED $REMOTE
    keepbackup=false
[alias]
	lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --
